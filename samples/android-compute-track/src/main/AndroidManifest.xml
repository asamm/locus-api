<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!-- Info screen — explains what the app exposes and how to select it as a router in Locus. -->
        <activity
            android:name=".InfoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            The routing provider. Locus discovers it purely by the ACTION_COMPUTE_TRACK_PROVIDER
            intent-filter (no meta-data, no permission) and binds it on demand to call computeTrack.
            Capabilities (attribution, profiles, transit-point count, settings) are reported over
            AIDL at bind time, not declared here.
        -->
        <service
            android:name=".WavyRouteService"
            android:exported="true"
            android:label="@string/service_name">

            <intent-filter>
                <action android:name="locus.api.android.ACTION_COMPUTE_TRACK_PROVIDER" />
            </intent-filter>
        </service>

    </application>

</manifest>
