<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!-- Info screen — explains what the app exposes and how to pair it in Locus. -->
        <activity
            android:name=".InfoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            The adapter itself. Locus owns the BT4 scan + GATT lifecycle; this service only
            decodes byte frames. See InfoActivity / HrmAdapterService.kt for the parser and
            res/xml/locus_adapter.xml for the device-type catalogue Locus reads pre-bind.
        -->
        <service
            android:name=".HrmAdapterService"
            android:exported="true"
            android:icon="@drawable/ic_hrm"
            android:label="@string/service_hrm_name"
            android:permission="com.asamm.locus.permission.SENSOR_ADAPTER">

            <intent-filter>
                <action android:name="locus.api.android.ACTION_SENSOR_ADAPTER_PARSER" />
            </intent-filter>

            <meta-data
                android:name="locus.api.android.SENSOR_ADAPTER"
                android:resource="@xml/locus_adapter" />
        </service>

    </application>

</manifest>
